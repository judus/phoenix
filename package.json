{
  "name": "phoenix",
  "version": "0.1.0",
  "private": true,
  "license": "PolyForm-Strict-1.0.0",
  "type": "module",
  "engines": {
    "node": ">=24.14.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @phoenix/contracts && npm run build --workspace @phoenix/elite && npm run build --workspace @phoenix/copilot && npm run build --workspace @phoenix/ui && npm run build --workspace @phoenix/server && npm run build --workspace @phoenix/web",
    "catalogue:check": "node scripts/catalogue/refresh.mjs --output data/runtime/catalogue --max-age-hours 24",
    "catalogue:refresh": "node scripts/catalogue/refresh.mjs --output data/runtime/catalogue --force",
    "check": "npm run typecheck && npm test && npm run build",
    "dev": "node scripts/dev.mjs",
    "dev:server": "npm run dev --workspace @phoenix/server",
    "dev:web": "npm run dev --workspace @phoenix/web",
    "storybook": "npm run storybook --workspace @phoenix/storybook",
    "storybook:build": "npm run build-storybook --workspace @phoenix/storybook",
    "pretest": "npm run build --workspace @phoenix/contracts && npm run build --workspace @phoenix/elite && npm run build --workspace @phoenix/copilot",
    "payload:build": "npm run build && node scripts/package/build-payload.mjs",
    "payload:smoke": "node scripts/package/smoke-test-payload.mjs",
    "payload:verify": "node scripts/package/verify-payload.mjs",
    "start": "npm run start --workspace @phoenix/server",
    "test": "vitest run",
    "typecheck": "npm run build --workspace @phoenix/contracts && npm run build --workspace @phoenix/elite && npm run build --workspace @phoenix/copilot && npm run typecheck --workspace @phoenix/ui && npm run typecheck --workspace @phoenix/server && npm run typecheck --workspace @phoenix/web"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
